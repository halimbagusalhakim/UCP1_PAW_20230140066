<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Daftar pengunjung</title>
  <link
    rel="stylesheet"
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
  />
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-3">Daftar pengunjung</h1>
    <p>Ini adalah halaman daftar pengunjung.</p>

    <!-- Form Tambah Tugas -->
    <form action="/add-todo" method="POST" class="mb-4">
      <div class="input-group">
        <input
          type="text"
          name="task"
          class="form-control"
          placeholder="Tambahkan Nama baru..."
          required
        />
        <div class="input-group-append">
          <button class="btn btn-success" type="submit">Tambah Tugas</button>
        </div>
      </div>
    </form>

    <!-- Daftar Tugas -->
    <% if (todos && todos.length > 0) { %>
      <ul class="list-group">
        <% todos.forEach(function(todo) { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span><b>Nama:</b> <%= todo.task %></span>
            <div class="btn-group" role="group">

              <!-- Form Edit -->
              <form action="/update-todo/<%= todo.id %>" method="POST" class="mr-2">
                <input type="text" name="task" value="<%= todo.task %>" class="form-control form-control-sm d-inline-block w-auto" required />
                <button class="btn btn-warning btn-sm" type="submit">Edit</button>
              </form>
              
              <!-- Form Hapus -->
              <form action="/delete-todo/<%= todo.id %>" method="POST" onsubmit="return confirm('Yakin ingin menghapus tugas ini?')">
                <button class="btn btn-danger btn-sm" type="submit">Hapus</button>
              </form>

            </div>
          </li>
        <% }); %>
      </ul>
    <% } else { %>
      <p class="text-muted">Tidak ada pengunjung yang tersedia.</p>
    <% } %>

    <!-- Kembali ke Home -->
    <a href="/" class="btn btn-secondary mt-3">Kembali ke Home</a>
  </div>
</body>
</html>